# AGENTS v2（课程版）

## 1. 课程定位

《LoongArch汇编语言》是人工智能专业群系统软件课程线的第一门核心课程。

课程以**LoongArch架构**为基础，以**miniOS工程**为实践载体，以**龙芯2K0300**为目标平台，采用"知识学习---工程实践---AI共学"三线融合模式。

课程链路：

``` text
LoongArch汇编
    ↓
miniOS
    ↓
Agent Runtime
    ↓
Agent OS
    ↓
龙芯智能体开发
```

> 本文档既是课程实施规范，也是Codex实验开发规范。

------------------------------------------------------------------------

# 2. 课程组织规则

本课程资料按**课程周次**组织，而不是按学校行政教学周组织。

统一规则：

``` text
1 个课程周次 = 2 节课
整门课程 = 16 个课程周次 = 32 节课
```

如果实际排课为一周 4 节课，则一个行政教学周通常对应两个课程周次：

``` text
行政第 1 周
├── 第 1-2 节：课程第 1 周
└── 第 3-4 节：课程第 2 周
```

如果实际排课为一周 2 节课，则一个行政教学周对应一个课程周次。调课、补课或合班授课时，只说明本次课讲授的课程周次，例如“本次完成 week03 和 week04”，不修改资料编号。

课程结构详见：

- `docs/course_structure.md`

------------------------------------------------------------------------

# 3. 三线融合教学模式

每周均采用统一结构：

## 一、课程知识（Why）

-   建立知识体系
-   理解为什么学习本周内容
-   明确与整门课程的位置关系

## 二、课堂Demo（How）

教师演示完整工程过程： - 编译 - 反汇编 - 调试 - QEMU运行 - 结果分析

## 三、实验实践（Do）

学生完成最小可验证实验。

## 四、AI共学

利用AI： - 解释源码 - 分析错误 - 绘制流程图 - 理解运行机制

第 1 周仅体验 AI 辅助理解，不允许直接生成实验代码。第 2 周开始允许在教师规定范围内使用 AI 辅助分析错误、解释反汇编和整理实验报告，但实验结果必须来自真实命令输出。

## 五、思考与拓展

课程讨论、实验报告与课后阅读。

------------------------------------------------------------------------

# 4. 16 周课程主线

| 课程周次 | 主题 |
|---|---|
| 第 1 周 | 从 0 启动 LoongArch miniOS |
| 第 2 周 | `.data/.bss` 初始化与 C/汇编混合启动 |
| 第 3 周 | 分支、循环与字符串输出 |
| 第 4 周 | 函数调用约定与栈帧 |
| 第 5 周 | Makefile、链接脚本与镜像结构 |
| 第 6 周 | GDB 调试与反汇编分析 |
| 第 7 周 | 系统调用 `sys_write` |
| 第 8 周 | 异常入口与异常上下文 |
| 第 9 周 | 中断基础与定时器 |
| 第 10 周 | `memset/memcpy` 与基础库函数 |
| 第 11 周 | UART 驱动整理与设备抽象 |
| 第 12 周 | miniOS 内核服务整理 |
| 第 13 周 | QEMU 到 2K0300 移植准备 |
| 第 14 周 | 2K0300 启动与 UART 适配 |
| 第 15 周 | Agent Runtime 初步接入 |
| 第 16 周 | 综合实验：从 miniOS 到 Agent OS |

------------------------------------------------------------------------

# 5. 第一周：从0启动一个LoongArch miniOS

## 课程主题

> 从0启动一个LoongArch miniOS

## （一）课程知识

学习目标：

-   为什么学习汇编
-   为什么选择LoongArch
-   为什么采用QEMU
-   miniOS整体结构
-   裸机程序启动流程

课程路线：

``` text
CPU
 ↓
boot.S
 ↓
main()
 ↓
printk()
 ↓
UART
 ↓
Hello miniOS
```

------------------------------------------------------------------------

## （二）课堂Demo

教师演示：

1.  工程目录结构
2.  Makefile编译
3.  objdump反汇编
4.  QEMU运行
5.  GDB单步观察启动流程

最终输出：

``` text
Hello miniOS on LoongArch64
```

------------------------------------------------------------------------

## （三）实验实践

Task1 环境检查

-   make
-   qemu-system-loongarch64
-   loongarch64-linux-gnu-gcc

Task2 工程认识

认识：

-   boot/
-   kernel/
-   drivers/
-   include/
-   linker.ld

Task3 运行Hello miniOS

执行：

``` bash
make
make run
```

Task4 阅读boot.S

思考：

1.  CPU第一条指令在哪里？
2.  为什么不是main？
3.  为什么不能直接printf？

------------------------------------------------------------------------

## （四）AI共学

第一周仅体验：

-   AI解释boot.S作用
-   AI绘制miniOS启动流程
-   AI解释QEMU作用

不允许直接生成实验代码。

------------------------------------------------------------------------

## （五）思考题

1.  为什么操作系统必须从汇编开始？
2.  为什么先QEMU再开发板？
3.  boot.S负责解决什么问题？

------------------------------------------------------------------------

# 6. 第二周：.data/.bss 初始化与 C/汇编混合启动

## 课程主题

> 让裸机 C 程序拥有可信的全局变量初始状态

学习目标：

- 区分 `.text`、`.rodata`、`.data`、`.bss`
- 理解为什么 `.bss` 必须在进入 C 代码前清零
- 观察 `boot/start.S` 中的 `clear_bss`
- 理解 C 代码如何调用汇编实现的 `memset`、`memcpy`、`strlen`
- 通过 QEMU 输出验证 `.data/.bss` 行为

第二周验收输出：

``` text
Hello miniOS on LoongArch64
data section ok
bss section cleared
week1-week2 check done
```

说明：当前代码路径会同时保留第 1 周 Hello 输出，第二周重点验收后三行 `.data/.bss` 检查结果。

------------------------------------------------------------------------

# 7. 原有工程规范

保留原AGENTS所有工程规范，包括：

-   项目目录
-   工具链
-   Makefile规范
-   QEMU规范
-   2K0300移植规范
-   测试规范
-   编码规范
-   禁止事项

上述内容保持不变，仅作为课程工程实施规范使用。

------------------------------------------------------------------------

# 8. 课件制作规范

-   PPT正文字号不得小于24磅（标题、正文、代码块、表格等所有文字内容均适用这一下限），保证教室后排可读。
